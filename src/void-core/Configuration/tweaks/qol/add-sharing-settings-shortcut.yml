---
title: Add Network Sharing Shortcut
description: Adds a network sharing settings shortcut to the Void 'File Sharing' folder for QoL
actions:
  - !powerShell:
    command: |
      .\VoidModules\initPowerShell.ps1
      New-Shortcut -Source 'control.exe' `
        -Destination """$([Environment]::GetFolderPath('Windows'))\VoidDesktop\3. General Configuration\File Sharing\Sharing Settings.lnk""" `
        -Arguments '/name Microsoft.NetworkAndSharingCenter /page Advanced'
    exeDir: true
    wait: true

# VOID-OS-HASH-EVASION-a27e9ff1-5fa2-4af6-8275-65aaa1664bdc
